# Sample apps (aggregator)


## Build, Run, Bake

```shell
# cleanup previous build
./gradlew clean

# build application
./gradlew build

# execute application
./gradlew bootRun

# bake container image
./gradlew jib --image=<container registry>

```
